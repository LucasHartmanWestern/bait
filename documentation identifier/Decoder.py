import torch
import torch.nn as nn


class Decoder(nn.Module):
    """
def __init__(self,
                trg_vocab_size,
                embed_size,
                num_layers,
                heads,
                forward_expansion,
                dropout,
                device,
                max_length,
    ):
"""


#def forward(self, x, enc_out, src_mask, trg_mask):
