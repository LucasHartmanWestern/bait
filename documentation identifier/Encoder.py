import torch
import torch.nn as nn


class Encoder(nn.Module):
    """
    def __init__(
            self,
            source_vocab_size,
            embed_size,
            num_layers,
            heads,
            device,
            forward_expansion,
            dropout,
            max_length,
    ):
    """
# def forward(self, x, mask):
